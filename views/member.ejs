<%- include ("templates/header") %>
    <div class="container">
      <h1>Hello, <%= username %></h1>
      <br>
      <div class="container text-center">
        <div class="row">
          <% for (let i = 0; i < images.length; i++) { %>
            <div class="col">
              <img src="/images/<%= images[i] %>" alt="cat image <%= images[i] %>">
            </div>
            <% } %>  
        </div>
      </div>
      <br>
      <a href="/logout">Logout</a>
    </div>
<%- include ("templates/footer") %>